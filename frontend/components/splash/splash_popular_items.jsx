import React from "react";
import { withRouter, Link } from "react-router-dom";

const SplashPopularItems = () => {
  return (
    <div>
      <p className="splash-popular-wrapper">Popular right now</p>
    </div>
  );
};

export default withRouter(SplashPopularItems);
